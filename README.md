# SandboxRepo

Throwaway repo for epic-runner sandbox runs. Its CI (`ci.sh`) fails while a `BROKEN` file exists;
the epic-runner stub (`stubs/claude.ts`) creates and removes it to exercise the CI fix loop.
